import "./Title.css"; // This is standard practice to keep import statmements at the TOF.

function Title() {
  return <h1 className="Title">This is the Title.</h1>;
}

export default Title; // This is standard practice to keep export statements at the EOF.
